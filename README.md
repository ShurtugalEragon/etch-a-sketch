# etch-a-sketch
This is an etch-a-sketch like drawing app that uses mouse activity to change the color where
the mouse hovers

It uses a default of 16 x 16 divs to create the drawing canvas with the option to increase the 
number of divs up to 100, allowing for a smaller paint brush size